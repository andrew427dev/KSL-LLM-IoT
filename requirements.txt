# ── PC / 서버 환경 (훈련 + 개발용) ──────────────────────
# Raspberry Pi 배포 전용 패키지는 requirements-rpi.txt 참조

# Core CV
mediapipe>=0.10.0
opencv-python>=4.8.0

# ML (훈련)
tensorflow>=2.13.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Gemini API (신 SDK — google-generativeai는 EOL)
google-genai>=0.3.0

# TTS
gTTS>=2.3.0
pyttsx3>=2.90
pygame>=2.5.0

# Utilities
python-dotenv>=1.0.0
pandas>=2.0.0
matplotlib>=3.7.0
tqdm>=4.65.0
