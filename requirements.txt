# ── PC / 서버 환경 (훈련 + 개발용) ──────────────────────
# Raspberry Pi 배포 전용 패키지는 requirements-rpi.txt 참조

# Core CV
# mediapipe 0.10.30대부터 legacy solutions API(mp.solutions.hands) 제거 — 상한 고정
mediapipe>=0.10.0,<0.10.30
opencv-python>=4.8.0

# ML (훈련)
# TF 2.16+(Keras 3)는 LSTM의 TFLite 변환이 MLIR 오류로 실패하고,
# TF 2.17+는 protobuf 5.x를 요구해 mediapipe(protobuf<5)와 충돌 — 2.15대 고정
tensorflow>=2.13.0,<2.16
numpy>=1.24.0,<2.0
scikit-learn>=1.3.0

# Gemini API (신 SDK — google-generativeai는 EOL)
google-genai>=0.3.0

# TTS
gTTS>=2.3.0
pyttsx3>=2.90
pygame>=2.5.0

# Utilities
python-dotenv>=1.0.0
pandas>=2.0.0
matplotlib>=3.7.0
tqdm>=4.65.0
